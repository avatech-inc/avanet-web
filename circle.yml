machine:
  node:
    version: 5.4.1

test:
  pre:
    - npm install -g jasmine-node

deployment:
  master:
    branch: master
    commands:
      - npm run build
      - ./scripts/upload_source_maps.sh
      - scp -r ./build/* ubuntu@52.91.103.60:~/prod
  develop:
    branch: develop
    commands:
      - npm run build:develop
      - scp -r ./build/* ubuntu@52.91.103.60:~/develop
